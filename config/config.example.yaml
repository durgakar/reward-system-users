campaign_id: spring_rewards_2026

# File-based mode for local dry-run without PostgreSQL
rules_source: file
segments_source: file
shopping_source: csv
shopping_data_path: data/sample_clients.csv
rules_file: config/rules.yaml
segments_file: config/segments.yaml

templates_dir: templates
email_sender: stdout
reward_provider: ledger
dry_run: true

# Production / Docker mode — switch to database sources:
# rules_source: database
# segments_source: database
# shopping_source: database
# reward_provider: db_ledger
# dry_run: false
# database:
#   driver: postgres
#   url: postgres://reward:reward@localhost:5432/reward_system?sslmode=disable

server:
  port: 8080
  admin_api_key: dev-key

cron:
  enabled: false
