campaign_id: docker_production

rules_source: database
segments_source: database
shopping_source: database
templates_dir: templates
email_sender: smtp
reward_provider: db_ledger
dry_run: false

database:
  driver: postgres
  url: postgres://reward:reward@postgres:5432/reward_system?sslmode=disable

server:
  host: 0.0.0.0
  port: 8080
  admin_api_key: dev-key

cron:
  enabled: true
  schedule: "0 9 * * *"

smtp:
  host: mailhog
  port: 1025
  from: rewards@example.com

open_loyalty:
  base_url: http://openloyalty:8181/api
  api_key: ""
  store_code: default

voucherify:
  base_url: https://api.voucherify.io/v1
  application_id: ""
  secret_key: ""
  loyalty_id: ""
